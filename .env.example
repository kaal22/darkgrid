# Backend: required for IP threat data (globe + feed). Get a key at https://www.abuseipdb.com/
ABUSEIPDB_API_KEY=

# Frontend (local dev only): point at backend. Docker uses build-time default.
NEXT_PUBLIC_THREAT_API_URL=http://localhost:8000
